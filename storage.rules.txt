rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // instagram-media フォルダ内のファイルへのアクセス
    match /instagram-media/{allPaths=**} {
      // 開発環境: 全てのアクセスを許可
      allow read, write: if true;
      
      // 本番環境では以下のように認証とファイルサイズ制限を追加推奨
      // allow read: if true;
      // allow write: if request.auth != null 
      //              && request.resource.size < 50 * 1024 * 1024; // 50MB制限
    }
  }
}